import React, { useState } from "react";
import axios from "axios";

function HomePage() {
  // ... everything from your current App.js stays the same here
  // (copy all of your state, handlers, and return block)
}

export default HomePage;
